plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

dependencies {
    // (opcional) adicione libs extras se quiser, mas Swing já está no Java
}

application {
    mainClass = 'alphacontrol.App'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}
